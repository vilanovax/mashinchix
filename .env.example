# PostgreSQL + Redis با Docker (از ریشهٔ پروژه):
#   docker compose up -d
#
# تنظیم واقعی Prisma در api/.env — نمونهٔ کامل: api/.env.example

DATABASE_URL="postgresql://mashinchi:mashinchi@localhost:5432/mashinchi?schema=public"
REDIS_URL="redis://localhost:6379"
